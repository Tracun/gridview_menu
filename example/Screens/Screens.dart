import 'package:flutter/cupertino.dart';

// Your Own screen
class AdministrativeScreen extends StatelessWidget {
  const AdministrativeScreen({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}

// Your Own screen
class SchedulingScreen extends StatelessWidget {
  const SchedulingScreen({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}

// Your Own screen
class ScheduleListScreen extends StatelessWidget {
  const ScheduleListScreen({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}